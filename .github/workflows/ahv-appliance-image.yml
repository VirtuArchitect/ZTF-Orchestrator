name: Build AHV Appliance Image

on:
  workflow_dispatch:
    inputs:
      source_ref:
        description: Git ref to bake into the appliance source checkout
        required: false
        default: main
      image_version:
        description: ZTF-Orchestrator container tag to preload and run
        required: false
        default: latest
      qemu_accelerator:
        description: QEMU accelerator to use; tcg works on GitHub-hosted runners
        required: false
        default: tcg
      build_container_image:
        description: Build the ZTF-Orchestrator image inside the QCOW2 instead of relying on GHCR
        required: false
        default: "true"
      pull_container_images:
        description: Pull supporting container images into the QCOW2 when network is available
        required: false
        default: "true"
      ztf_framework_ref:
        description: ZeroTouch Framework 1.x ref to bake into the container image
        required: false
        default: v1.5.2
  push:
    tags: [ "v*" ]

permissions:
  contents: write

jobs:
  build-qcow2:
    name: Build AHV QCOW2
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v6

      - name: Resolve build inputs
        id: vars
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            echo "source_ref=${{ inputs.source_ref }}" >> "$GITHUB_OUTPUT"
            echo "image_version=${{ inputs.image_version }}" >> "$GITHUB_OUTPUT"
            echo "qemu_accelerator=${{ inputs.qemu_accelerator }}" >> "$GITHUB_OUTPUT"
            echo "build_container_image=${{ inputs.build_container_image }}" >> "$GITHUB_OUTPUT"
            echo "pull_container_images=${{ inputs.pull_container_images }}" >> "$GITHUB_OUTPUT"
            echo "ztf_framework_ref=${{ inputs.ztf_framework_ref }}" >> "$GITHUB_OUTPUT"
            echo "artifact_suffix=${{ inputs.source_ref }}" >> "$GITHUB_OUTPUT"
          else
            echo "source_ref=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "image_version=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "qemu_accelerator=tcg" >> "$GITHUB_OUTPUT"
            echo "build_container_image=true" >> "$GITHUB_OUTPUT"
            echo "pull_container_images=true" >> "$GITHUB_OUTPUT"
            echo "ztf_framework_ref=v1.5.2" >> "$GITHUB_OUTPUT"
            echo "artifact_suffix=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          fi

      - name: Install QEMU
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 qemu-utils

      - name: Set up Packer
        uses: hashicorp/setup-packer@v3
        with:
          version: latest

      - name: Initialize Packer
        working-directory: appliance/packer
        run: packer init ahv-qcow2.pkr.hcl

      - name: Validate Packer template
        working-directory: appliance/packer
        run: |
          packer validate \
            -var "version=${{ steps.vars.outputs.source_ref }}" \
            -var "ztf_orchestrator_version=${{ steps.vars.outputs.image_version }}" \
            -var "qemu_accelerator=${{ steps.vars.outputs.qemu_accelerator }}" \
            -var "build_container_image=${{ steps.vars.outputs.build_container_image }}" \
            -var "pull_container_images=${{ steps.vars.outputs.pull_container_images }}" \
            -var "ztf_framework_ref=${{ steps.vars.outputs.ztf_framework_ref }}" \
            ahv-qcow2.pkr.hcl

      - name: Build AHV QCOW2
        working-directory: appliance/packer
        run: |
          packer build \
            -var "version=${{ steps.vars.outputs.source_ref }}" \
            -var "ztf_orchestrator_version=${{ steps.vars.outputs.image_version }}" \
            -var "qemu_accelerator=${{ steps.vars.outputs.qemu_accelerator }}" \
            -var "build_container_image=${{ steps.vars.outputs.build_container_image }}" \
            -var "pull_container_images=${{ steps.vars.outputs.pull_container_images }}" \
            -var "ztf_framework_ref=${{ steps.vars.outputs.ztf_framework_ref }}" \
            ahv-qcow2.pkr.hcl

      - name: Package image checksum
        working-directory: appliance/packer
        run: |
          sha256sum output/*.qcow2 > output/SHA256SUMS

      - name: Upload QCOW2 artifact
        uses: actions/upload-artifact@v4
        with:
          name: ztf-orchestrator-ahv-qcow2-${{ steps.vars.outputs.artifact_suffix }}
          path: |
            appliance/packer/output/*.qcow2
            appliance/packer/output/SHA256SUMS
          if-no-files-found: error

      - name: Attach QCOW2 to release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1 \
            || gh release create "${GITHUB_REF_NAME}" --title "${GITHUB_REF_NAME}" --generate-notes
          gh release upload "${GITHUB_REF_NAME}" appliance/packer/output/*.qcow2 appliance/packer/output/SHA256SUMS --clobber
